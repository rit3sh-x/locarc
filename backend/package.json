{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "logs": "convex logs",
    "seed": "tsx scripts/seed.ts",
    "lint": "tsc -p tsconfig.json && eslint . --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "@better-auth/expo": "1.5.3",
    "@convex-dev/better-auth": "^0.11.4",
    "better-auth": "1.5.3",
    "convex": "^1.34.1"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "dotenv": "^17.4.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}