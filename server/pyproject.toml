[project]
name = "compute"
version = "0.1.0"
description = "Compute for HackRF frequencies to coordinates"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "numpy",
    "scipy",
    "httpx",
    "pydantic",
    "pydantic-settings",
    "python-dotenv",
]
